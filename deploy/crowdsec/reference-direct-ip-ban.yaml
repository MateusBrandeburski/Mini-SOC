type: trigger
name: custom/direct-ip-access
description: "Acesso direto pelo IP (scanner) - ban imediato"
filter: "evt.Line.Labels.direct_ip == 'true'"
groupby: "evt.Meta.source_ip"
blackhole: 10s
labels:
  remediation: true
  type: scan
  service: http
